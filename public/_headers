# Cloudflare Pages headers.
# https://developers.cloudflare.com/pages/configuration/headers/

/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  # Allow embedding only on Story Advantage sites (add your WordPress domain).
  # TODO(confirm): replace/extend with the real {{WORDPRESS_DOMAIN}}.
  Content-Security-Policy: frame-ancestors 'self' https://storyadvantage.co https://www.storyadvantage.co
